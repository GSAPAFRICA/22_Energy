# Cloudflare's documented test widget. It works on localhost only with the
# matching test secret in .dev.vars (copy .dev.vars.example to .dev.vars).
NEXT_PUBLIC_TURNSTILE_SITE_KEY=1x00000000000000000000AA
